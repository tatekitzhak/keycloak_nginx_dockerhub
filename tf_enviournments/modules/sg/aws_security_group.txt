resource "aws_security_group" "allow_tls" {
  name        = "allow_tls"
  description = "Allow TLS inbound traffic and all outbound traffic"
  vpc_id      = aws_vpc.main.id

  tags = {
    Name = "allow_tls"
  }
}

# Ingress rule that allows cidr_ipv4 https
resource "aws_vpc_security_group_ingress_rule" "allow_tls_ipv4" {
  security_group_id = aws_security_group.allow_tls.id
  cidr_ipv4         = aws_vpc.main.cidr_block
  from_port         = 443
  to_port           = 443
  ip_protocol       = "tcp"
}

# Ingress rule that allows cidr_ipv6 https 
resource "aws_vpc_security_group_ingress_rule" "allow_tls_ipv6" {
  security_group_id = aws_security_group.allow_tls.id
  cidr_ipv6         = aws_vpc.main.ipv6_cidr_block
  from_port         = 443
  to_port           = 443
  ip_protocol       = "tcp"
}

# Ingress rule that allows cidr_ipv4 http
resource "aws_vpc_security_group_ingress_rule" "example" {
  security_group_id = aws_security_group.example.id

  cidr_ipv4   = "10.0.0.0/8"
  from_port   = 80
  to_port     = 80
  ip_protocol = "tcp"
}

# Engress rule that allows cidr_ipv4 http
resource "aws_vpc_security_group_egress_rule" "example" {
  security_group_id = aws_security_group.example.id

  cidr_ipv4   = "10.0.0.0/8"
  from_port   = 80
  to_port     = 80
  ip_protocol = "tcp"
}


# Ingress rule that allows SSH access from the entity's external gateway IP
resource "aws_vpc_security_group_ingress_rule" "bastion_ssh_office" {
  security_group_id            = aws_security_group.bastion.id
  cidr_ipv4                    = "1.2.3.4/32"
  from_port                    = 22
  to_port                      = 22
  ip_protocol                  = "tcp"
}

# Egress rule that allows all access cidr_ipv4
resource "aws_vpc_security_group_egress_rule" "allow_all_traffic_ipv4" {
  security_group_id = aws_security_group.allow_tls.id
  cidr_ipv4         = "0.0.0.0/0"
  ip_protocol       = "-1" # semantically equivalent to all ports
}


# Egress rule that allows all access cidr_ipv4
resource "aws_vpc_security_group_egress_rule" "bastion_all" {
  security_group_id            = aws_security_group.bastion.id
  cidr_ipv4                    = "0.0.0.0/0"
  from_port                    = -1
  ip_protocol                  = -1
  to_port                      = -1
}

# Egress rule that allows all access cidr_ipv6
resource "aws_vpc_security_group_egress_rule" "allow_all_traffic_ipv6" {
  security_group_id = aws_security_group.allow_tls.id
  cidr_ipv6         = "::/0"
  ip_protocol       = "-1" # semantically equivalent to all ports
}