#!/bin/bash

docker-compose down --remove-orphans
docker-compose build
docker-compose up

# docker-compose docker-compose.yaml down --remove-orphans
# docker-compose docker-compose.yaml build
# docker-compose docker-compose.yaml up

if [ $? -eq 0 ]; then
  echo "Success!"
else
  echo "failed!"
fi
